spring:
  datasource:
    url: jdbc:mysql://${DATABASE_HOST}:3306/ESP?serverTimezone=Europe/Moscow
    username: ${DATABASE_USER_NAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update